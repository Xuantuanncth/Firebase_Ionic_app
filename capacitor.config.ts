import { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = {
  appId: "io.ionic.demo.pg.cap.ng",
  appName: "Control Device",
  bundledWebRuntime: false,
  npmClient: "npm",
  webDir: "www",
};

export default config;
